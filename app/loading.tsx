import { BrandLoadingScreen } from "@/components/ui/BrandLoadingScreen";

/** Root App Router fallback shown while the page segment is streaming. */
export default function Loading() {
  return <BrandLoadingScreen />;
}
